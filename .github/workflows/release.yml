name: Release

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag (e.g. v1.2.3)'
        required: true
        type: string

jobs:
  wait-for-builds:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        workflow: [ build-x86_64.yml, build-aarch64.yml ]
    steps:
      - name: Wait for ${{ matrix.workflow }} to finish on main
        uses: lewagon/wait-on-check-action@v1.3.4
        with:
          ref: main
          check-name: build
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          wait-interval: 15
          allowed-conclusions: success

  release:
    needs: wait-for-builds
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Download x86_64 artifact
        uses: dawidd6/action-download-artifact@v6
        with:
          workflow: build-x86_64.yml
          branch: main
          name: kpart-x86_64
          path: dist/x86_64

      - name: Download aarch64 artifact
        uses: dawidd6/action-download-artifact@v6
        with:
          workflow: build-aarch64.yml
          branch: main
          name: kpart-aarch64
          path: dist/aarch64

      - name: Package artifacts
        run: |
          cd dist
          zip -r buildcharge-x86_64.zip x86_64/
          zip -r buildcharge-aarch64.zip aarch64/

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ inputs.tag }}
          name: ${{ inputs.tag }}
          draft: false
          prerelease: false
          generate_release_notes: true
          files: |
            dist/buildcharge-x86_64.zip
            dist/buildcharge-aarch64.zip